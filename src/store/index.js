import { createStore } from 'vuex'

export default createStore({
  state: {
    uid: null,
    choice: "signin",
    axios: require('axios'),
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
